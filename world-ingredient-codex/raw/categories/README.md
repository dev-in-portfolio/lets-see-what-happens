# Expandable Category Buckets

These folders group ingredient categories where parent items can be expanded into meaningful sub-types.
Use each `expandable_groups.txt` file as a seed list for deeper codex expansion work.
